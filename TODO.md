# Pending before this repo is fully presentable

Assets to drop into `assets/`:
- `logo.png` — 256×256, transparent background
- `demo.gif` — ≤8s loop, ≤4 MB, one full conflict-resolve flow
- `screenshot-conflict.png` — ~1600×1000, VS Code conflicted file view
- `screenshot-resolved.png` — ~1600×1000, gitfix panel with rationale

Other follow-ups:
- Confirm the VS Code Marketplace badge URL once the extension is actually published (current ID assumed: `ameyypawar.gitfix`). The badge will read "n/a" until then.
- Replace the `gitfix.pro/privacy` link once that page exists.
- Update `v0.1.0` changelog with the real release date when shipped.
